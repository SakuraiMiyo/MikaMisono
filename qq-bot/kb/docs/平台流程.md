# 未花 · 平台流程（已改由 Skill 承载）

> ⚠️ **这份文档的内容已经搬进 AstrBot 的 Skill 系统了。**
> Skill 是**确定性**的——提示词里直接给文件路径，我读一下就拿到，不靠相似度召回。
> 所以「用的时候一定要查得到」的东西，现在都放在 Skill 里，不放知识库。

## 去哪找

| 要做什么 | 读哪个 SKILL.md |
|---|---|
| 给老师点麦当劳 | `~\.astrbot\data\skills\mika-delivery\SKILL.md` |
| 用 ComfyUI 给老师画图 | `~\.astrbot\data\skills\mika-image\SKILL.md` |
| 发说说 / 给老师的动态写评论 | `~\.astrbot\data\skills\mika-qq-space\SKILL.md` |
| 写当天的日记 | `~\.astrbot\data\skills\mika-diary\SKILL.md` |

（`~` = `C:\Users\MisonoMika`）

## 为什么搬走了

1. **可靠性**：知识库是向量相似度召回，**可能召不回来**；Skill 是"提示词里给了路径 → 我去读"，**不会漏**。
2. **省 token**：这几份手册原本常驻 `system_prompt` 占 4,000+ 字，其实只在"老师让我做事"的那一刻才用得上。
   Skill 的 progressive disclosure 只把**名称 + 描述 + 路径**放进提示词（每个约 90 字），正文按需读。

## ⚠️ 别在这里维护第二份

**这四份流程的唯一真源是上面那四个 `SKILL.md`。**
本文件曾经抄过一份，而且里面的「写评论」还是**旧的 event 驱动流程**——
2026-09-18 之后评论已经改成 `llm_view_feed` → 自己写正文 → `llm_comment_feed` 两步走了。
留着一份过期的只会让模型照着错的走，所以正文已全部删除。

> 源文件保留在项目里：`C:\OneDrive\MikaMisono\qq-bot\kb\docs\平台流程.md`
> （也就是本文件——它现在只是指路牌，不再是手册。）
